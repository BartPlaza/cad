import React from 'react'
import { t } from '../../helpers/I18n';

const testView = () => {
    return (
        <div className="container">
            {t('test')}
        </div>
    )
};

export default testView;